--- src/position.rs
+++ replace - with + in Position<I, D, BaseOrQuote>::change_position
@@ -160,17 +160,17 @@
                         debug_assert_eq!(
                             transaction_accounting
                                 .margin_balance_of(USER_POSITION_MARGIN_ACCOUNT)
                                 .expect("Is valid account"),
                             BaseOrQuote::PairedCurrency::zero()
                         );
                     }
                     Ordering::Greater => {
-                        let new_short_qty = filled_qty - inner.quantity();
+                        let new_short_qty = filled_qty + /* ~ changed by cargo-mutants ~ */ inner.quantity();
                         inner.decrease_contracts(
                             inner.quantity(),
                             fill_price,
                             transaction_accounting,
                             init_margin_req,
                             1,
                             fees,
                         );
