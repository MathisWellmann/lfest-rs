--- src/types/smol_currency/base_currency.rs
+++ replace % with / in <impl std::ops::Rem for BaseCurrency<I, D>>::rem
@@ -228,17 +228,17 @@
 impl<I, const D: u8> std::ops::Rem for BaseCurrency<I, D>
 where
     I: Mon<D>,
 {
     type Output = Self;
 
     #[inline]
     fn rem(self, rhs: Self) -> Self::Output {
-        Self(self.0 % rhs.0)
+        Self(self.0 / /* ~ changed by cargo-mutants ~ */ rhs.0)
     }
 }
 
 impl<I, const D: u8> std::fmt::Display for BaseCurrency<I, D>
 where
     I: Mon<D>,
 {
     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
