--- src/types/smol_currency/quote_currency.rs
+++ replace <impl One for QuoteCurrency<I, D>>::one -> Self with Default::default()
@@ -151,17 +151,17 @@
 }
 
 impl<I, const D: u8> One for QuoteCurrency<I, D>
 where
     I: Mon<D>,
 {
     #[inline]
     fn one() -> Self {
-        Self(Decimal::<I, D>::try_from_scaled(I::one(), 0).unwrap())
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 
     #[inline]
     fn set_one(&mut self) {
         *self = One::one();
     }
 
     #[inline]
